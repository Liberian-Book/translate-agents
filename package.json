{
  "scripts": {
    "build": "python3 agents/agent-archive/scripts/build-preview.py ../entrepreneurship && cp -R ../entrepreneurship/.html/* ../web-site/entrepreneurship/",
    "deploy": "npx wrangler pages deploy .html --project-name openstax-vn"
  },
  "dependencies": {
    "axios": "^1.16.0",
    "cheerio": "^1.2.0",
    "html-to-docx": "^1.8.0",
    "puppeteer": "^24.43.0"
  }
}
